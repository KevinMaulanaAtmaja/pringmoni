# prisma migrate pake direct url
npx prisma migrate dev --url="$DIRECT_URL"

# prisma studio pake direct url
npx prisma studio --url="$DIRECT_URL"

# shadcn preset
npx shadcn@latest init --preset b3ubQPikny --template next

# cek status, migrate, seed
npx prisma migrate status
npx prisma db push/pull
npx prisma db seed
npx prisma generate
npx tsx prisma/testPrisma.ts


list
-midleware bedsarkan role, session login expired
-meja dipake ga bisa buat pesen orang lain, tpi bsa jika pake hp yg sama pesen lagi
-nama pelanggan di saat pembayaran, orderid randomin berdasarkan waktu pesan aja,
-tampil nama pelanggan di kartu pesanan aktif+struk 
-customer bisa akses /dashboard, role lain sesuai fungsi
-Create Pesanan Detail Page (/dashboard/pesanan/[id]) → Lihat detail pesanan
-laporan page, create pesanan page(kasir)
-konsisten ada error dibwh input, minimal huruf ditiap2 input
-sanitasi input, hak akses tiap halaman role
- halaman cara bayar, connect ke midtrans